[project]
name = "tfg-codigo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib==3.10.8",
    "more-itertools==11.0.1",
    "numpy==2.4.3",
    "pandas==3.0.2",
    "pyarrow==22.0.0",
    "scipy==1.18.0",
    "statsmodels==0.14.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gwaslib"]
